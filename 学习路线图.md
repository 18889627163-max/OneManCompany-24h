# OneManCompany 深度学习路线图

> 从零到精通的完整学习路径

---

## 📚 学习路线总览

```
第一阶段: 核心概念 (2-3小时)
   ↓
第二阶段: 数据流与执行流程 (3-4小时)
   ↓
第三阶段: 关键模块深入 (5-6小时)
   ↓
第四阶段: 实战与扩展 (开放式)
```

---

## 🎯 第一阶段：核心概念理解

### 1.1 三大核心概念

#### **Vessel（躯壳）**
- **定义**：员工的执行容器，定义"如何运行"
- **位置**：`src/onemancompany/core/vessel.py`
- **核心职责**：
  - 任务调度与执行
  - 重试策略与超时控制
  - 工具权限管理
  - 通信协议

**关键代码片段**：
```python
# vessel.py:788-823
class Vessel:
    """Per-employee view into the EmployeeManager.
    
    Per-employee view providing task management and history access.
    """
    def __init__(self, manager: "EmployeeManager", employee_id: str):
        self.manager = manager
        self.employee_id = employee_id
        self.agent = _VesselRef(employee_id)
```

**理解要点**：
- Vessel 是 EmployeeManager 的一个视图（View）
- 不持有业务数据，只是指针和接口
- 通过 `manager` 委托所有实际操作

---

#### **Talent（灵魂）**
- **定义**：能力包，带来技能、知识、性格
- **位置**：`src/onemancompany/talent_market/`
- **结构**：
```
talent_package/
├── talent.yaml       # 元信息
├── skills/           # 技能文件
├── tools/            # 专属工具
└── agent.py          # 可选的自定义 agent
```

---

#### **Employee（员工）= Vessel + Talent**
- **公式**：`Employee = Vessel（执行容器）+ Talent（能力包）`
- **类比**：高达 = 机体 + 驾驶员
- **实现**：通过 `EmployeeManager` 完成组合

```
employees/00010/           # 员工目录
├── profile.yaml           # 档案（姓名、角色、部门）
├── vessel/                # 躯壳配置
│   ├── vessel.yaml        # DNA：runner, hooks, limits
│   └── prompt_sections/   # 提示词片段
├── skills/                # 已安装的技能
└── progress.log           # 工作记忆（跨任务持久化）
```

---

### 1.2 执行模式（Executor/Launcher）

系统支持 3 种执行后端，通过 `Launcher` 协议统一：

#### **1. LangChainExecutor（公司托管）**
```python
# vessel.py:651-673
class LangChainExecutor(Launcher):
    """Executes tasks via a LangChain react agent."""
    
    def __init__(self, agent_runner: BaseAgentRunner):
        self.agent = agent_runner
    
    async def execute(self, task_description: str, context: TaskContext, ...) -> LaunchResult:
        result = await self.agent.run_streamed(task_description, on_log=on_log)
        # 返回 output, tokens, cost
```

**特点**：
- 通过 OpenRouter 调用 LLM
- 完全由 OMC 管理（重试、日志、成本追踪）
- 开箱即用，只需 API Key

---

#### **2. ClaudeSessionExecutor（自托管）**
```python
# vessel.py:676-714
class ClaudeSessionExecutor(Launcher):
    """Executes tasks via Claude CLI sessions."""
    
    async def execute(...):
        from onemancompany.core.claude_session import run_claude_session
        result = await run_claude_session(
            self.employee_id,
            context.project_id,
            prompt=task_description,
            work_dir=context.work_dir,
            ...
        )
```

**特点**：
- 需要安装 Claude CLI + Pro 订阅
- 能力更强（Claude Code 完整工具链）
- 更省 token（Anthropic 原生缓存）

---

#### **3. ScriptExecutor（扩展接口）**
```python
# vessel.py:717-765
class ScriptExecutor(Launcher):
    """Executes tasks via a custom bash script."""
    
    # 执行 employees/{id}/launch.sh
    # 通过 stdin 传入任务，stdout 返回结果
```

**用途**：接入任意自定义 Agent（Python、Go、Rust...）

---

### 1.3 EmployeeManager：中央调度器

**核心设计哲学**：
```python
# vessel.py:12-17
"""
Design:
  No persistent while-loop per employee — tasks execute on-demand.
  When a task is pushed, EmployeeManager creates a one-shot asyncio.Task.
  When that task completes, the next pending task is auto-scheduled.
  Between tasks, no process/coroutine is occupied.
"""
```

**零空转架构**：
- ❌ 没有 `while True` 轮询
- ✅ 事件驱动：有任务才执行
- ✅ 任务完成后自动调度下一个
- ✅ 空闲时无任何协程占用

**关键数据结构**：
```python
# vessel.py:1021-1040
class EmployeeManager:
    def __init__(self):
        self.executors: dict[str, Launcher] = {}              # 执行后端
        self.vessels: dict[str, Vessel] = {}                  # Vessel 视图
        self.configs: dict[str, VesselConfig] = {}            # DNA 配置
        self.task_histories: dict[str, list[dict]] = {}       # 历史记录
        self._running_tasks: dict[str, asyncio.Task] = {}     # 运行中的任务
        self._schedule: dict[str, list[ScheduleEntry]] = {}   # 待执行队列
        self._current_entries: dict[str, ScheduleEntry] = {}  # 当前执行项
```

---

### 1.4 任务树（TaskTree）与节点（TaskNode）

**任务树结构**：
```yaml
# task_tree.yaml 示例
root_id: T001
nodes:
  T001:
    id: T001
    description: "开发一个解谜游戏"
    employee_id: "00002"  # COO
    status: "completed"
    children_ids: [T002, T003, T004]
  
  T002:
    id: T002
    parent_id: T001
    description: "游戏设计文档"
    employee_id: "00015"  # 设计师
    status: "accepted"
  
  T003:
    id: T003
    parent_id: T001
    description: "开发游戏逻辑"
    employee_id: "00021"  # 工程师
    status: "processing"
    depends_on: [T002]  # 依赖设计文档
```

**任务状态机**：
```
PENDING → PROCESSING → COMPLETED → ACCEPTED
                ↓
            HOLDING（暂停等待）
                ↓
            FAILED / CANCELLED
```

**关键点**：
- 树存储在 `projects/{project_id}/task_tree.yaml`
- 每个节点对应一个员工的一次任务执行
- 父节点负责分派子任务（通过 `dispatch_child` 工具）
- 依赖关系：`depends_on` 确保执行顺序

---

## 🔄 第二阶段：数据流与执行流程

### 2.1 任务生命周期完整流程

```
1. CEO 在浏览器输入任务
      ↓
2. Frontend 发送 POST /task
      ↓
3. routes.py 创建根节点（TaskTree）
      ↓
4. EmployeeManager.push_task(EA_ID, ...)
      ↓
5. schedule_node() → _schedule.append(ScheduleEntry)
      ↓
6. _schedule_next() → 创建 asyncio.Task(_run_task)
      ↓
7. _execute_task() → launcher.execute(task_description)
      ↓
8. Agent 执行（调用工具、分派子任务）
      ↓
9. 完成后更新 node.status = "completed"
      ↓
10. _schedule_next() → 自动执行下一个任务
```

---

### 2.2 核心调度逻辑

#### **任务推送**：
```python
# vessel.py:1332-1350
def push_task(self, employee_id: str, description: str, 
              project_id: str, node_id: str, tree_path: str) -> str:
    """Push a task for an employee. Returns node_id."""
    if node_id and tree_path:
        self.schedule_node(employee_id, node_id, tree_path)
    self._schedule_next(employee_id)
    return node_id
```

#### **调度逻辑**：
```python
# vessel.py:1356-1384
def _schedule_next(self, employee_id: str):
    """If no task is running, start the next scheduled one."""
    if employee_id in self._running_tasks:
        return  # 已有任务在执行
    
    entry = self.get_next_scheduled(employee_id)
    if not entry:
        # 无待办任务 → 设为 IDLE
        self._set_employee_status(employee_id, STATUS_IDLE)
        return
    
    # 创建 asyncio.Task 开始执行
    loop = asyncio.get_running_loop()
    self._running_tasks[employee_id] = loop.create_task(
        self._run_task(employee_id, entry)
    )
```

#### **任务执行**：
```python
# vessel.py:1684-1698
async def _run_task(self, employee_id: str, entry: ScheduleEntry):
    """Execute a task, then schedule the next one."""
    self._current_entries[employee_id] = entry
    try:
        await self._execute_task(employee_id, entry)
    finally:
        # 清理 + 调度下一个
        self._current_entries.pop(employee_id, None)
        self._running_tasks.pop(employee_id, None)
        self._schedule_next(employee_id)  # 🔄 自动调度
```

---

### 2.3 Stall Detection（停滞检测）

**问题**：Agent 说"我将分配任务"，但实际没调用 `dispatch_child` 工具

**解决方案**：正则检测 + 任务树验证

```python
# vessel.py:147-174
_PROMISE_PATTERNS = re.compile(
    r"(?:"
    r"我将|接下来|下一步|现在开始"  # 中文
    r"|I will (?:now |start )|Let me"  # 英文
    r")",
    re.IGNORECASE,
)

def detect_unfulfilled_promises(output: str | None) -> bool:
    """检查 Agent 输出是否包含未兑现的承诺"""
    if not output:
        return False
    return bool(_PROMISE_PATTERNS.search(output))
```

**验证逻辑**：
```python
# vessel.py:267-291
def _should_retry_stall(node, initial_child_ids, verified_child_ids) -> bool:
    """任务完成后检查是否需要因停滞而重试"""
    # 1. 检查未兑现的承诺
    unfulfilled = not node.children_ids and detect_unfulfilled_promises(node.result)
    
    # 2. 检查虚假的分派声明（说分配了 8 个任务，实际只有 2 个）
    unverified = detect_unverified_dispatch_claim(
        node.result, node.children_ids, initial_child_ids, verified_child_ids
    )
    
    if unfulfilled or unverified:
        return getattr(node, 'stall_retry_count', 0) < MAX_STALL_RETRIES
    return False
```

**真实案例**：
```
Agent 输出: "我已经分配了 8 个子任务"
实际结果: task_tree.yaml 只有 2 个子节点
判定: 停滞 → 自动重试（最多 2 次）
```

---

## 🏗️ 第三阶段：关键模块深入

### 3.1 Vessel Config（DNA 配置）

**文件位置**：`employees/{id}/vessel/vessel.yaml`

```yaml
# 示例 vessel.yaml
runner:
  module: "onemancompany.agents.base"
  class: "BaseAgentRunner"

hooks:
  pre_task: "onemancompany.hooks.log_task_start"
  post_task: "onemancompany.hooks.save_summary"

context:
  sections:
    - "role_identity"
    - "task_tree"
    - "progress_log"
  max_progress_lines: 30

limits:
  max_retries: 3
  retry_delays: [5, 15, 30]
  max_subtask_depth: 2
  timeout_seconds: 600

capabilities:
  sandbox: true
  file_upload: true
  websocket: false
```

**加载逻辑**：
```python
# 位置：src/onemancompany/core/vessel_config.py
class VesselConfig:
    def __init__(self, employee_id: str):
        self.employee_id = employee_id
        self.runner = ...
        self.hooks = ...
        self.context = ...
        self.limits = ...
        self.capabilities = ...
    
    @classmethod
    def load(cls, employee_id: str) -> VesselConfig:
        """从 vessel.yaml 加载配置"""
        path = EMPLOYEES_DIR / employee_id / "vessel" / "vessel.yaml"
        # ... 解析 YAML
```

---

### 3.2 六大 Harness 协议

Vessel 通过 **Harness（套接件）** 与公司系统解耦：

| Harness | 职责 | 典型方法 |
|---------|------|----------|
| **ExecutionHarness** | 执行适配 | `execute()`, `is_ready()` |
| **TaskHarness** | 任务队列 | `push()`, `get_next()`, `cancel()` |
| **EventHarness** | 事件发布 | `publish()`, `subscribe()` |
| **StorageHarness** | 持久化 | `save_progress()`, `load_history()` |
| **ContextHarness** | 上下文组装 | `build_prompt()`, `get_dependencies()` |
| **LifecycleHarness** | 生命周期钩子 | `on_task_start()`, `on_task_complete()` |

**为什么需要 Harness？**
- 解耦：Vessel 不直接依赖 EventBus、CompanyState
- 可测试：Mock Harness 即可单测
- 可扩展：未来可替换底层实现

---

### 3.3 EventBus（事件总线）

**位置**：`src/onemancompany/core/events.py`

**核心机制**：异步 Pub/Sub

```python
class EventBus:
    def __init__(self):
        self._subscribers: dict[EventType, list[Callable]] = {}
    
    async def publish(self, event: CompanyEvent):
        """发布事件到所有订阅者"""
        handlers = self._subscribers.get(event.type, [])
        for handler in handlers:
            await handler(event)
    
    def subscribe(self, event_type: EventType, handler: Callable):
        """订阅事件"""
        self._subscribers.setdefault(event_type, []).append(handler)

# 全局单例
event_bus = EventBus()
```

**使用场景**：
```python
# 发布任务完成事件
await event_bus.publish(CompanyEvent(
    type=EventType.TASK_COMPLETED,
    employee_id="00021",
    data={"node_id": "T003", "result": "..."}
))

# WebSocket 订阅并推送到前端
event_bus.subscribe(EventType.TASK_COMPLETED, websocket_handler)
```

---

### 3.4 Progress Log（工作记忆）

**核心思想**：跨任务持久化的简短日志

**文件位置**：`employees/{id}/progress.log`

```
[2026-08-13T10:23:45] Task T001 completed: game design doc drafted
[2026-08-13T10:45:12] Task T003 started: implement game logic
[2026-08-13T11:02:33] Found bug in collision detection, fixing...
```

**写入**：
```python
# vessel.py:834-841
def _append_progress(employee_id: str, entry: str):
    """Append an entry to the employee's progress log."""
    path = EMPLOYEES_DIR / employee_id / PROGRESS_LOG_FILENAME
    path.parent.mkdir(parents=True, exist_ok=True)
    with open(path, "a", encoding="utf-8") as f:
        f.write(f"[{datetime.now().isoformat()[:19]}] {entry}\n")
```

**读取与注入**：
```python
# vessel.py:995-1005
def _load_progress(employee_id: str, max_lines: int = 30) -> str:
    """Load recent entries from the employee's progress log."""
    path = EMPLOYEES_DIR / employee_id / PROGRESS_LOG_FILENAME
    if not path.exists():
        return ""
    lines = read_text_utf(path).strip().split("\n")
    return "\n".join(lines[-max_lines:])  # 只取最近 30 行

# 注入到 Agent 的系统提示词
progress_context = _load_progress(employee_id)
system_prompt += f"\n\n## Recent Work Context\n{progress_context}"
```

**与 task_history 的区别**：
- `progress.log`：简短、手动写入、跨项目可见
- `task_history.json`：结构化、自动生成、按任务聚合

---

## 🎯 第四阶段：实战练习

### 练习 1：追踪一次完整任务执行

**目标**：理解从 CEO 输入到任务完成的全流程

**步骤**：
1. 在 `vessel.py:1356` 加断点（`_schedule_next`）
2. 启动项目：`bash start.sh`
3. 浏览器输入任务："写一个 Hello World Python 脚本"
4. 观察调用栈和数据流

**关键观察点**：
- `schedule_node` 何时被调用
- `_running_tasks` 的变化
- `TaskNode.status` 的状态转换
- EventBus 发布了哪些事件

---

### 练习 2：实现自定义 Launcher

**目标**：接入一个简单的 Python Agent

**步骤**：
1. 创建 `employees/99999/launch.sh`：
```bash
#!/bin/bash
# 读取任务描述（stdin）
read -r task

# 简单的硬编码响应
echo "收到任务: $task"
echo "执行结果: Hello from custom launcher!"
```

2. 注册 ScriptExecutor：
```python
from onemancompany.core.vessel import ScriptExecutor, employee_manager

launcher = ScriptExecutor("99999")
vessel = employee_manager.register("99999", launcher)
```

3. 推送任务测试：
```python
vessel.push_task("测试自定义启动器", project_id="test_001")
```

---

### 练习 3：分析 Stall Detection

**目标**：理解停滞检测的触发条件

**测试用例**：
```python
# 场景 1：未兑现的承诺
output = "我将立即分配 5 个子任务给团队"
children_ids = []  # 实际没有子任务

assert detect_unfulfilled_promises(output) == True
assert _should_retry_stall(node) == True

# 场景 2：虚假的分派声明
output = "已成功分配 8 个子任务"
children_ids = ["T001", "T002"]  # 只有 2 个

claim = _dispatch_claim_details(output)
assert claim == (8, False)  # 声称 8 个，非"本次新增"

assert detect_unverified_dispatch_claim(
    output, children_ids, verified_child_ids=children_ids
) == True
```

---

## 📖 推荐阅读顺序

### 第一批：基础设施（3 个文件）
1. ✅ **vessel.py** (您已打开) - 核心调度
2. **vessel_config.py** - DNA 配置
3. **events.py** - 事件总线

### 第二批：任务系统（4 个文件）
4. **task_tree.py** - 任务树数据结构
5. **task_lifecycle.py** - 状态机
6. **task_persistence.py** - 持久化
7. **tree_tools.py** - 任务工具（dispatch_child）

### 第三批：Agent 实现（4 个文件）
8. **agents/base.py** - BaseAgentRunner
9. **agents/coo_agent.py** - COO 示例
10. **agents/common_tools.py** - 共享工具
11. **claude_session.py** - Claude CLI 集成

### 第四批：前端与 API（3 个文件）
12. **api/routes.py** - REST API
13. **api/websocket.py** - 实时推送
14. **frontend/app.js** - CEO 控制台

---

## 🔍 调试技巧

### 1. 启用详细日志
```bash
export LOG_LEVEL=DEBUG
bash start.sh
```

### 2. 查看任务树
```bash
cat .onemancompany/company/business/projects/{project_id}/task_tree.yaml
```

### 3. 监控事件流
```python
# 在 events.py 添加全局日志
async def publish(self, event: CompanyEvent):
    logger.debug(f"[EVENT] {event.type.value}: {event.data}")
    # ...原有逻辑
```

### 4. 检查员工状态
```bash
cat .onemancompany/company/employees/00002/progress.log
cat .onemancompany/company/employees/00002/task_history.json
```

---

## 🎓 进阶主题

### 1. 依赖解析（Dependency Resolution）
- 位置：`vessel.py:_resolve_dependencies`
- 场景：任务 B 依赖任务 A 的结果
- 机制：`TaskNode.depends_on = ["A"]`

### 2. 持有状态（HOLDING）
- 场景：等待 CEO 回复、等待外部资源
- 实现：定时轮询 + 事件唤醒

### 3. 多 Agent 会议
- 位置：`agents/common_tools.py:meeting_tool`
- 机制：创建临时会议室，多个 Agent 同时执行

### 4. 成本追踪
- 位置：`project_archive.py`
- 统计：每个任务的 token 用量和 USD 成本

---

## ❓ 常见困惑解答

### Q1: Vessel 和 Employee 的区别？
**A**: 
- `Vessel` 是运行时的视图对象（轻量级）
- `Employee` 是概念：`Vessel（容器）+ Talent（能力）+ Profile（档案）`

### Q2: 为什么不用 LangGraph 的 Checkpointer？
**A**: 
- OMC 的任务树是**业务层**的抽象（人类可读）
- LangGraph 的 Checkpoint 是**运行时**状态（图执行中间态）
- 两者互补：任务树做审批流，Checkpoint 做故障恢复

### Q3: ScheduleEntry 和 TaskNode 的关系？
**A**: 
- `ScheduleEntry` 是纯指针（只有 node_id + tree_path）
- `TaskNode` 是完整的业务数据（描述、结果、状态）
- 设计原则：调度层不持有业务数据，保持轻量

---

## 📝 下一步行动

1. ⭐ **Star 这个项目**（支持开源）
2. 🏃 **运行一次完整流程**（CEO → EA → COO → 工程师）
3. 📖 **阅读推荐文件列表**（按顺序）
4. 🐛 **提交一个 Issue 或 PR**（参与贡献）
5. 💬 **加入社区**（扫描 README 中的二维码）

---

**制作时间**: 2026-08-13  
**适用版本**: v0.7.110  
**维护者**: 协助您学习的 AI 助手

Good luck! 🚀
